[project]
name = "khaliduo-backend-api"
version = "1.0.0"
description = "FastAPI backend for the Khaliduo employee time tracking system."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"
