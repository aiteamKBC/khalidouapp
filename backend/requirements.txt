fastapi==0.139.2
starlette==1.3.1
uvicorn[standard]==0.38.0
pydantic==2.12.5
pydantic-settings==2.14.2
SQLAlchemy==2.0.45
alembic==1.17.2
psycopg[binary]==3.3.2
python-multipart==0.0.31
PyJWT==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.3.0
structlog==25.5.0
# zoneinfo has no timezone database of its own on hosts that ship without one,
# and every timezone helper falls back to UTC in silence when a lookup fails.
tzdata==2026.3
pytest==9.0.3
httpx==0.28.1
Pillow==12.3.0
cryptography==48.0.1
